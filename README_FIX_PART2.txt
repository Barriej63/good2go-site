Drop these into your repo root so they land at:
- app/pricing/page.tsx
- app/privacy/page.tsx

Be sure styles/globals.css includes:
.page-actions { margin-top: 16px; }

Then commit and redeploy with "Clear build cache".
